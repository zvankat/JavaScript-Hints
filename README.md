# JavaScript-Hints
В данном репозитории представлены все про JS.

---

## 📦 Переменные

`var` - устаревший способ (функциональная область видимости)  
`let` - переменная (можно менять)  
`const` - константа (нельзя переприсвоить)

---

## 🏷️ Типы данных

### Примитивные
`number` - числа (целые, дробные, Infinity, NaN)  
`string` - строки ("текст", 'текст', `шаблон`)  
`boolean` - логический (true, false)  
`undefined` - значение не присвоено  
`null` - ничего (пусто)  
`symbol` - уникальный идентификатор  
`bigint` - большие числа (123n)

### Ссылочные
`object` - объект  
`array` - массив  
`function` - функция  
`date` - дата  
`regexp` - регулярное выражение  
`map` - коллекция ключ-значение  
`set` - коллекция уникальных значений  
`weakmap` - слабая ссылка на ключи  
`weakset` - слабая ссылка на значения

---

## 🔢 Числа и математика

`Number()` - преобразование в число  
`parseInt()` - целое число из строки  
`parseFloat()` - дробное число из строки  
`isNaN()` - проверка на NaN  
`isFinite()` - проверка на конечное число  
`toFixed()` - округление до знаков после запятой  
`toPrecision()` - округление до значащих цифр  
`Math.random()` - случайное число 0-1  
`Math.round()` - округление  
`Math.ceil()` - округление вверх  
`Math.floor()` - округление вниз  
`Math.abs()` - модуль  
`Math.pow()` - степень  
`Math.sqrt()` - корень  
`Math.min()` - минимум  
`Math.max()` - максимум  
`Math.PI` - число Пи

---

## 📝 Строки

`length` - длина строки  
`toUpperCase()` - в верхний регистр  
`toLowerCase()` - в нижний регистр  
`charAt()` - символ по индексу  
`indexOf()` - индекс первого вхождения  
`lastIndexOf()` - индекс последнего вхождения  
`includes()` - содержит ли подстроку  
`startsWith()` - начинается ли с  
`endsWith()` - заканчивается ли на  
`slice()` - вырезать кусок  
`substring()` - подстрока  
`substr()` - подстрока (устаревший)  
`split()` - разбить в массив  
`join()` - соединить массив в строку  
`trim()` - удалить пробелы по краям  
`trimStart()` - удалить пробелы в начале  
`trimEnd()` - удалить пробелы в конце  
`replace()` - заменить  
`replaceAll()` - заменить все  
`repeat()` - повторить  
`concat()` - объединить  
`padStart()` - дополнить слева  
`padEnd()` - дополнить справа  
`match()` - найти по регулярке  
`search()` - поиск по регулярке  
`template literal` - шаблонная строка `${}`

---

## ⚙️ Операторы

### Арифметические
`+` - сложение  
`-` - вычитание  
`*` - умножение  
`/` - деление  
`%` - остаток от деления  
`**` - возведение в степень  
`++` - инкремент (+1)  
`--` - декремент (-1)

### Присваивания
`=` - присвоить  
`+=` - прибавить и присвоить  
`-=` - отнять и присвоить  
`*=` - умножить и присвоить  
`/=` - разделить и присвоить  
`%=` - остаток и присвоить  
`**=` - степень и присвоить

### Сравнения
`==` - равно (с приведением типов)  
`===` - строго равно (без приведения)  
`!=` - не равно  
`!==` - строго не равно  
`>` - больше  
`<` - меньше  
`>=` - больше или равно  
`<=` - меньше или равно  
`? :` - тернарный оператор (условие ? да : нет)

### Логические
`&&` - И (и)  
`||` - ИЛИ (или)  
`!` - НЕ (не)  
`??` - нулевой оператор (если null/undefined, то другое)

### Побитовые
`&` - И  
`|` - ИЛИ  
`^` - исключающее ИЛИ  
`~` - НЕ  
`<<` - сдвиг влево  
`>>` - сдвиг вправо  
`>>>` - беззнаковый сдвиг

---

## 🔀 Условные конструкции

`if (условие) {}` - если  
`else {}` - иначе  
`else if () {}` - иначе если  
`switch (x) { case 1: break; default: }` - переключатель

---

## 🔄 Циклы

`for (let i = 0; i < 10; i++) {}` - классический  
`for (let key in object) {}` - по ключам объекта  
`for (let value of array) {}` - по значениям массива  
`while (условие) {}` - пока условие верно  
`do {} while (условие)` - выполнить, потом проверить  
`break` - прервать цикл  
`continue` - перейти к следующей итерации

---

## 📊 Массивы

`[]` - создание  
`length` - длина  
`push()` - добавить в конец  
`pop()` - удалить с конца  
`unshift()` - добавить в начало  
`shift()` - удалить с начала  
`concat()` - объединить массивы  
`join()` - соединить в строку  
`slice()` - скопировать часть  
`splice()` - удалить/вставить  
`indexOf()` - индекс элемента  
`lastIndexOf()` - последний индекс  
`includes()` - есть ли элемент  
`find()` - найти первый подходящий  
`findIndex()` - найти индекс первого подходящего  
`filter()` - отфильтровать  
`map()` - преобразовать каждый  
`reduce()` - свернуть (аккумулятор)  
`reduceRight()` - свернуть справа  
`forEach()` - перебрать  
`some()` - есть ли подходящий  
`every()` - все ли подходят  
`sort()` - отсортировать  
`reverse()` - перевернуть  
`fill()` - заполнить  
`flat()` - разгладить  
`flatMap()` - разгладить и преобразовать  
`Array.from()` - создать из итерируемого  
`Array.isArray()` - проверить массив ли  
`Array.of()` - создать из аргументов

---

## 📦 Объекты

`{}` - создание  
`object.key` - доступ через точку  
`object["key"]` - доступ через скобки  
`delete object.key` - удалить свойство  
`key in object` - проверить наличие  
`for (let key in object) {}` - перебор ключей  
`Object.keys()` - массив ключей  
`Object.values()` - массив значений  
`Object.entries()` - массив пар [ключ, значение]  
`Object.fromEntries()` - объект из пар  
`Object.assign()` - скопировать свойства  
`Object.freeze()` - заморозить (нельзя менять)  
`Object.seal()` - запечатать (нельзя добавлять/удалять)  
`Object.create()` - создать с прототипом  
`Object.defineProperty()` - определить свойство  
`Object.defineProperties()` - определить несколько  
`Object.getPrototypeOf()` - получить прототип  
`Object.setPrototypeOf()` - установить прототип

---

## 🎯 Функции

### Объявление
`function name() {}` - объявление функции  
`const name = function() {}` - функциональное выражение  
`const name = () => {}` - стрелочная функция  
`new Function()` - через конструктор (не рекомендуется)

### Параметры
`param` - обычный параметр  
`param = value` - параметр по умолчанию  
`...args` - остаточные параметры  
`arguments` - псевдомассив аргументов (не в стрелочных)

### Вызов
`name()` - вызов  
`new Name()` - как конструктор  
`name.call(this, arg1, arg2)` - с указанием this и аргументами  
`name.apply(this, [args])` - с this и массивом  
`name.bind(this)` - привязать контекст

### Свойства и методы
`name.length` - количество параметров  
`name.name` - имя функции  
`toString()` - код функции

---

## 🧬 Классы (ES6)

`class Name {}` - объявление класса  
`constructor() {}` - конструктор  
`method() {}` - метод  
`static method() {}` - статический метод  
`get prop() {}` - геттер  
`set prop(value) {}` - сеттер  
`extends` - наследование  
`super()` - вызов родителя  
`#private` - приватное поле  
`instanceof` - проверка на принадлежность классу

---

## ❗ Ошибки

`try {} catch (error) {} finally {}` - обработка ошибок  
`throw new Error()` - выбросить ошибку  
`Error` - базовая ошибка  
`SyntaxError` - синтаксическая ошибка  
`TypeError` - ошибка типа  
`ReferenceError` - ссылка на несуществующее  
`RangeError` - выход за диапазон  
`URIError` - ошибка URI  
`EvalError` - ошибка eval

---

## ⏱️ Таймеры

`setTimeout(fn, ms)` - выполнить один раз через ms  
`setInterval(fn, ms)` - выполнять каждые ms  
`clearTimeout(id)` - отменить setTimeout  
`clearInterval(id)` - отменить setInterval  
`requestAnimationFrame(fn)` - при следующей отрисовке  
`cancelAnimationFrame(id)` - отменить

---

## 📍 Контекст this

`this` - зависит от вызова:  
- в глобальном контексте - window/global  
- в функции - зависит от вызова  
- в методе объекта - объект  
- в конструкторе - новый объект  
- в стрелочной функции - внешний this  
- в обработчике события - элемент  
- bind/call/apply - что передали

---

## 🔗 Прототипы

`__proto__` - прототип (нестандартный)  
`Object.getPrototypeOf()` - получить прототип  
`Object.setPrototypeOf()` - установить прототип  
`prototype` - свойство функции-конструктора  
`isPrototypeOf()` - проверка прототипа  
`hasOwnProperty()` - свое свойство (не из прототипа)

---

## 📦 Модули (ES6)

`export` - экспорт  
`export default` - экспорт по умолчанию  
`import` - импорт  
`import * as name from` - импорт всего  
`import {name} from` - именованный импорт  
`import name from` - импорт по умолчанию  
`export {name}` - именованный экспорт  
`export {name as alias}` - экспорт с псевдонимом  
`import()` - динамический импорт

---

## 🌐 Promise и асинхронность

### Promise
`new Promise((resolve, reject) => {})` - создание  
`resolve(value)` - успех  
`reject(error)` - ошибка  
`.then(result => {})` - обработка успеха  
`.catch(error => {})` - обработка ошибки  
`.finally(() => {})` - всегда  
`Promise.all([...])` - все промисы  
`Promise.allSettled([...])` - все завершенные  
`Promise.race([...])` - кто быстрее  
`Promise.any([...])` - первый успешный  
`Promise.resolve()` - промис с успехом  
`Promise.reject()` - промис с ошибкой

### Async/await
`async function() {}` - асинхронная функция  
`await promise` - ждать промис

### Остальное
`setTimeout` - таймаут  
`setInterval` - интервал  
`queueMicrotask()` - микрозадача

---

## 🛜 Работа с DOM

### Поиск элементов
`document.getElementById()` - по ID  
`document.getElementsByClassName()` - по классу  
`document.getElementsByTagName()` - по тегу  
`document.querySelector()` - первый по CSS-селектору  
`document.querySelectorAll()` - все по CSS-селектору

### Навигация
`parentNode` - родитель  
`childNodes` - дети (включая текст)  
`children` - дети (только элементы)  
`firstChild` - первый ребенок  
`lastChild` - последний ребенок  
`previousSibling` - предыдущий сосед  
`nextSibling` - следующий сосед  
`previousElementSibling` - предыдущий элемент  
`nextElementSibling` - следующий элемент

### Создание и изменение
`document.createElement()` - создать элемент  
`document.createTextNode()` - создать текст  
`element.innerHTML` - HTML внутри  
`element.outerHTML` - HTML с самим элементом  
`element.textContent` - текст внутри  
`element.innerText` - видимый текст  
`appendChild()` - добавить ребенка  
`insertBefore()` - вставить перед  
`removeChild()` - удалить ребенка  
`replaceChild()` - заменить  
`cloneNode()` - клонировать  
`append()` - добавить в конец  
`prepend()` - добавить в начало  
`after()` - добавить после  
`before()` - добавить перед  
`remove()` - удалить  
`replaceWith()` - заменить

### Атрибуты
`getAttribute()` - получить атрибут  
`setAttribute()` - установить атрибут  
`removeAttribute()` - удалить атрибут  
`hasAttribute()` - проверить атрибут  
`attributes` - коллекция атрибутов  
`classList` - список классов: add, remove, toggle, contains  
`dataset` - data-атрибуты

### Стили
`style` - инлайн стили  
`getComputedStyle()` - вычисленные стили  
`className` - классы строкой

### Размеры и позиция
`offsetWidth` / `offsetHeight` - ширина/высота с рамками  
`clientWidth` / `clientHeight` - без рамок  
`scrollWidth` / `scrollHeight` - с прокруткой  
`offsetTop` / `offsetLeft` - позиция относительно родителя  
`getBoundingClientRect()` - позиция относительно окна  
`scrollTop` / `scrollLeft` - прокрутка  
`scrollIntoView()` - прокрутить к элементу

---

## 🖱️ События

### Добавление
`element.addEventListener()` - добавить обработчик  
`element.removeEventListener()` - удалить  
`element.onclick = fn` - напрямую (один обработчик)

### Объект события
`event.type` - тип  
`event.target` - элемент, на котором сработало  
`event.currentTarget` - элемент с обработчиком  
`event.preventDefault()` - отменить действие по умолчанию  
`event.stopPropagation()` - остановить всплытие  
`event.stopImmediatePropagation()` - остановить все

### Виды событий
#### Мышь
`click` - клик  
`dblclick` - двойной клик  
`mousedown` - нажатие  
`mouseup` - отпускание  
`mousemove` - движение  
`mouseover` / `mouseout` - наведение/уход (всплывает)  
`mouseenter` / `mouseleave` - наведение/уход (не всплывает)  
`contextmenu` - контекстное меню

#### Клавиатура
`keydown` - нажатие  
`keyup` - отпускание  
`keypress` - устаревший

#### Формы
`submit` - отправка  
`focus` - фокус  
`blur` - потеря фокуса  
`input` - ввод  
`change` - изменение  
`reset` - сброс

#### Документ/окно
`load` - загрузка  
`DOMContentLoaded` - DOM готов  
`resize` - изменение размера  
`scroll` - прокрутка  
`beforeunload` - перед уходом

#### Touch
`touchstart` - касание  
`touchmove` - движение пальцем  
`touchend` - убрал палец

#### Drag & Drop
`dragstart` - начало перетаскивания  
`drag` - перетаскивание  
`dragend` - конец  
`dragenter` - вошел на цель  
`dragover` - над целью  
`dragleave` - ушел с цели  
`drop` - сброс

---

## 🌐 BOM (Browser Object Model)

`window` - глобальный объект  
`navigator` - информация о браузере  
`screen` - экран  
`location` - URL  
`history` - история  
`localStorage` - локальное хранилище (навсегда)  
`sessionStorage` - сессионное хранилище (до закрытия)  
`cookie` - куки  
`fetch()` - запросы  
`XMLHttpRequest` - старый способ запросов  
`alert()` - сообщение  
`confirm()` - подтверждение (ок/отмена)  
`prompt()` - ввод  
`open()` - открыть окно  
`close()` - закрыть  
`print()` - печать  
`requestAnimationFrame()` - анимация

---

## 🔄 JSON

`JSON.stringify()` - объект в JSON-строку  
`JSON.parse()` - JSON-строку в объект

---

## 🔒 Структуры данных

### Map
`new Map()` - создать  
`set(key, value)` - установить  
`get(key)` - получить  
`has(key)` - проверить  
`delete(key)` - удалить  
`clear()` - очистить  
`size` - размер  
`keys()` - ключи  
`values()` - значения  
`entries()` - пары  
`forEach()` - перебор

### Set
`new Set()` - создать  
`add(value)` - добавить  
`has(value)` - проверить  
`delete(value)` - удалить  
`clear()` - очистить  
`size` - размер  
`values()` - значения

### WeakMap / WeakSet
- ключи только объекты  
- нет перебора  
- сборщик мусора удаляет

---

## 📅 Дата и время

`new Date()` - текущая дата  
`new Date(2024, 0, 1)` - создать  
`getFullYear()` - год  
`getMonth()` - месяц (0-11)  
`getDate()` - день месяца  
`getDay()` - день недели (0-6)  
`getHours()` - часы  
`getMinutes()` - минуты  
`getSeconds()` - секунды  
`getMilliseconds()` - миллисекунды  
`getTime()` - миллисекунд с 1970  
`setFullYear()` - установить год  
`toISOString()` - ISO строка  
`toLocaleString()` - локализованная строка  
`Date.now()` - текущее время в мс  
`Date.parse()` - разобрать строку даты

---

## 🧰 Регулярные выражения

`/pattern/` - литерал  
`new RegExp()` - конструктор  
`test()` - проверить совпадение  
`exec()` - найти совпадения  
`match()` - найти в строке  
`search()` - найти индекс  
`replace()` - заменить  
`split()` - разбить

### Флаги
`i` - регистронезависимый  
`g` - глобальный (все совпадения)  
`m` - многострочный  
`s` - точка включает перенос  
`u` - юникод  
`y` - липкий поиск

### Символы
`.` - любой символ  
`\d` - цифра  
`\D` - не цифра  
`\w` - буква/цифра/_  
`\W` - не буква/цифра/_  
`\s` - пробельный  
`\S` - не пробельный  
`\b` - граница слова  
`\B` - не граница

### Квантификаторы
`*` - 0 и более  
`+` - 1 и более  
`?` - 0 или 1  
`{n}` - ровно n  
`{n,}` - n и более  
`{n,m}` - от n до m

### Группы
`()` - группа  
`(?:)` - не запоминающая группа  
`(?=)` - положительный просмотр вперед  
`(?!)` - отрицательный просмотр вперед  
`(?<=)` - положительный просмотр назад  
`(?<!)` - отрицательный просмотр назад

---

## 🎯 Итераторы и генераторы

`[Symbol.iterator]` - сделать итерируемым  
`next()` - следующий элемент  
`function*` - генератор  
`yield` - вернуть значение  
`yield*` - делегировать

---

## 🔧 Декораторы и прокси

`Proxy` - перехват операций  
`Reflect` - методы для Proxy  
`get` / `set` - ловушки  
`apply` - вызов  
`construct` - new

---

## 💤 Сон (эмуляция)

`await new Promise(r => setTimeout(r, ms))` - заснуть на ms

---

## 🧪 Отладка

`console.log()` - вывести  
`console.error()` - ошибка  
`console.warn()` - предупреждение  
`console.info()` - информация  
`console.table()` - таблицей  
`console.group()` / `groupEnd()` - группа  
`console.time()` / `timeEnd()` - время  
`console.trace()` - стек вызовов  
`console.count()` - счетчик  
`console.clear()` - очистить  
`debugger` - точка останова

---
